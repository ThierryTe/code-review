import { ProductDisplayPipe } from './product-display.pipe';

describe('ProductDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
